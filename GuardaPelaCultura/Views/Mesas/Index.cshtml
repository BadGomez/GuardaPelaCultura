@model IEnumerable<GuardaPelaCultura.Models.Mesa>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Restaurantes)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LugaresRestaurante)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MesasRestaurante)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Restaurantes.EmailRestaurante)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LugaresRestaurante)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MesasRestaurante)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MesaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MesaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MesaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
