@model IEnumerable<GuardaPelaCultura.Models.ReservasTakeAway>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NomeRestaurante)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NumeroTelefone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descrição)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NomeRestaurante)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NumeroTelefone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descrição)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ReservasTakeAwayId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ReservasTakeAwayId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ReservasTakeAwayId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
